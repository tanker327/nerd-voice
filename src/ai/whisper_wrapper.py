# import whisper

# model = whisper.load_model("large")
# index = 11 # number of fi

# def transcript(file_path, prompt=None):
#     result = model.transcribe(clip, initial_prompt=prompt)
#     return resulta
